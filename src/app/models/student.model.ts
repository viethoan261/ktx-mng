export interface Student {
  id: string;
  username: string;
  fullname: string;
  email: string;
  phone: string;
  roomNumber: string;
  role: string;
  password: string;
  // Thêm các thuộc tính khác nếu cần (ví dụ: studentId, major, etc.)
}
